import { FobiafilterPipe } from './fobiafilter.pipe';

describe('FobiafilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FobiafilterPipe();
    expect(pipe).toBeTruthy();
  });
});
